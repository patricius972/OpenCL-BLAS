- think about a better test structure for OpenCL kernels outside of OpenCL to 
  use standard debuggers
- re-think the alignment of the OpenCL code within the project to make reuse
  easier and to avoid duplication
